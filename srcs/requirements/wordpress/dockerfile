FROM alpine:3.15

RUN apk update && apk upgrade && apk add php7 php7-fpm php7-mysqli
RUN mkdir -p /usr/share/webapps/ && cd /usr/share/webapps/ && wget http://wordpress.org/latest.tar.gz && tar -xzvf latest.tar.gz && rm latest.tar.gz

#RUN chmod 755 /var/www/html

